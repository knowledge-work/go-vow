# testdata

Fixtures consumed by `golang.org/x/tools/go/analysis/analysistest`.
Active fixtures live under `internal/analysis/testdata/src/`, grouped by
preset and scenario; each preset's rule engine claims its own subdirectory
under that tree.
